<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
    <id>https://Liangxi2001.github.io</id>
    <title>凉汐博客</title>
    <updated>2022-06-10T09:17:50.961Z</updated>
    <generator>https://github.com/jpmonette/feed</generator>
    <link rel="alternate" href="https://Liangxi2001.github.io"/>
    <link rel="self" href="https://Liangxi2001.github.io/atom.xml"/>
    <subtitle>但行前路  莫问西东</subtitle>
    <logo>https://Liangxi2001.github.io/images/avatar.png</logo>
    <icon>https://Liangxi2001.github.io/favicon.ico</icon>
    <rights>All rights reserved 2022, 凉汐博客</rights>
    <entry>
        <title type="html"><![CDATA[Redis的基本数据类型]]></title>
        <id>https://Liangxi2001.github.io/post/redis-de-ji-ben-shu-ju-lei-xing/</id>
        <link href="https://Liangxi2001.github.io/post/redis-de-ji-ben-shu-ju-lei-xing/">
        </link>
        <updated>2022-06-10T09:12:18.000Z</updated>
        <content type="html"><![CDATA[<p><ul class="markdownIt-TOC">
<li>
<ul>
<li><a href="#string">String</a>
<ul>
<li><a href="#%E7%AE%80%E4%BB%8B">简介</a></li>
<li><a href="#%E5%B8%B8%E7%94%A8%E5%91%BD%E4%BB%A4">常用命令</a></li>
<li><a href="#%E6%95%B0%E6%8D%AE%E7%BB%93%E6%9E%84">数据结构</a></li>
</ul>
</li>
<li><a href="#list">List</a>
<ul>
<li><a href="#%E7%AE%80%E4%BB%8B-2">简介</a></li>
<li><a href="#%E5%B8%B8%E7%94%A8%E5%91%BD%E4%BB%A4-2">常用命令</a></li>
<li><a href="#%E6%95%B0%E6%8D%AE%E7%BB%93%E6%9E%84-2">数据结构</a></li>
</ul>
</li>
<li><a href="#set">Set</a>
<ul>
<li><a href="#%E7%AE%80%E4%BB%8B-3">简介</a></li>
<li><a href="#%E5%B8%B8%E7%94%A8%E5%91%BD%E4%BB%A4-3">常用命令</a></li>
<li><a href="#%E6%95%B0%E6%8D%AE%E7%BB%93%E6%9E%84-3">数据结构</a></li>
</ul>
</li>
<li><a href="#hash">Hash</a>
<ul>
<li><a href="#%E7%AE%80%E4%BB%8B-4">简介</a></li>
<li><a href="#%E5%B8%B8%E7%94%A8%E5%91%BD%E4%BB%A4-4">常用命令</a></li>
<li><a href="#%E6%95%B0%E6%8D%AE%E7%BB%93%E6%9E%84-4">数据结构</a></li>
</ul>
</li>
<li><a href="#zset">Zset</a>
<ul>
<li><a href="#%E7%AE%80%E4%BB%8B-5">简介</a></li>
<li><a href="#%E5%B8%B8%E7%94%A8%E5%91%BD%E4%BB%A4-5">常用命令</a></li>
<li><a href="#%E6%95%B0%E6%8D%AE%E7%BB%93%E6%9E%84-5">数据结构</a></li>
</ul>
</li>
</ul>
</li>
</ul>
</p>
<h2 id="string">String</h2>
<h3 id="简介">简介</h3>
<ul>
<li>Redis最基本的数据类型，一个key对应一个value</li>
<li>String类型是<strong>二进制安全</strong>的，即string可以包含任何数据，例如图片，视频或者序列化的对象</li>
<li>一个Redis的string类型的value最多可以是512M</li>
</ul>
<h3 id="常用命令">常用命令</h3>
<p>设置/取出key的值</p>
<pre><code class="language-shell">127.0.0.1:6379&gt; set k1 v100
OK
127.0.0.1:6379&gt; set k2 v100
OK
127.0.0.1:6379&gt; get k1
&quot;v100&quot;
#覆盖
127.0.0.1:6379&gt; set k1 v101
</code></pre>
<p>追加原始值</p>
<pre><code class="language-shell">127.0.0.1:6379&gt; APPEND k1 11
(integer) 6
127.0.0.1:6379&gt; get k1
&quot;v10011&quot;
</code></pre>
<p>查询某个key所对应的value的长度</p>
<pre><code class="language-shell">127.0.0.1:6379&gt; STRLEN k1
(integer) 6
</code></pre>
<p>添加没有的k-v</p>
<pre><code class="language-shell">127.0.0.1:6379&gt; SETNX k1
(error) ERR wrong number of arguments for 'setnx' command
127.0.0.1:6379&gt; setnx k3 100
(integer) 1
</code></pre>
<p>自增</p>
<pre><code class="language-shell">#增量为1
127.0.0.1:6379&gt; INCR k3
(integer) 101
#设置步长
127.0.0.1:6379&gt; INCRBY k3 10
(integer) 110
</code></pre>
<p>自减</p>
<pre><code class="language-shell">#减量为1
127.0.0.1:6379&gt; decr k3
(integer) 100
#设置步长
127.0.0.1:6379&gt; DECRBY k3 10
(integer) 100
</code></pre>
<p><strong>Redis单命令的原子性主要得益于Redis的单线程，Redis使用单线程+多路I/O复用。</strong></p>
<p><em>java中的i++操作不是原子性，当i=0时，两个线程分别对i进行++操作100次，所得的值处于2~200之间。</em></p>
<p>同时设置一个或多个k-v</p>
<pre><code class="language-shell">127.0.0.1:6379&gt; mset k1 v1 k2 v2 k3 v3
OK
</code></pre>
<p>同时获取一个或多个value</p>
<pre><code class="language-shell">127.0.0.1:6379&gt; mget k1 k2
1) &quot;v1&quot;
2) &quot;v2&quot;
</code></pre>
<p>同时设置一个或多个k-v，当且仅当给定key不存在</p>
<pre><code class="language-shell">127.0.0.1:6379&gt; msetnx k10 v10 k11 v11
(integer) 1
#失败的话不会有k-v被添加，此操作具有原子性
</code></pre>
<p>截取[v1,v2]之间的值，类似substring</p>
<pre><code class="language-shell">127.0.0.1:6379&gt; set name liangxi
OK
127.0.0.1:6379&gt; GETRANGE name 0 4
&quot;liang&quot;
</code></pre>
<p>覆盖从起始位置开始的值</p>
<pre><code class="language-shell">127.0.0.1:6379&gt; SETRANGE name 0 liu
(integer) 7
127.0.0.1:6379&gt; get name
&quot;liungxi&quot;
</code></pre>
<p>在添加新的k-v时设置过期时间</p>
<pre><code class="language-shell">127.0.0.1:6379&gt; SETEX temp 20 100  #过期时间是20s
OK
</code></pre>
<p>以新换旧，同时返回旧值</p>
<pre><code class="language-shell">127.0.0.1:6379&gt; GETSET name liangxi
&quot;liungxi&quot; 		#旧值
127.0.0.1:6379&gt; get name
&quot;liangxi&quot;
</code></pre>
<h3 id="数据结构">数据结构</h3>
<p>底层为简单动态字符串，是可以修改的字符串，内部结构实现类似于Java的ArrayList，采用预分配冗余空间的方式来减少内存的频繁分配。<strong>当字符串长度小于1M时，扩容是加倍现有的空间，当大于1M时，扩容只会在现有空间加1M，字符串最大长度为512M。</strong></p>
<h2 id="list">List</h2>
<h3 id="简介-2">简介</h3>
<ul>
<li>单键多值</li>
<li>是简单的字符串列表，按照插入的顺序排序，可以将一个元素添加到头部或者尾部</li>
<li>实际上是一个双向列表，对两端的操作性能高，但通过下标操作中间节点的性能较差</li>
</ul>
<h3 id="常用命令-2">常用命令</h3>
<p>从左边/右边插入一个或多个值</p>
<pre><code class="language-shell">127.0.0.1:6379&gt; LPUSH k1 v1 v2 v3
(integer) 3
127.0.0.1:6379&gt; RPUSH k2 v1 v2 v3
(integer) 3
</code></pre>
<p>查询指定范围的元素</p>
<pre><code class="language-shell"># 开始为0，结束为-1，意为查询所有元素
127.0.0.1:6379&gt; LRANGE k1 0 -1
1) &quot;v3&quot;
2) &quot;v2&quot;
3) &quot;v1&quot;
</code></pre>
<p>查询key中元素的个数</p>
<pre><code class="language-shell">127.0.0.1:6379&gt; llen k1
(integer) 3
</code></pre>
<p>根据下标查询元素</p>
<pre><code class="language-shell">127.0.0.1:6379&gt; LINDEX k1 1
&quot;v2&quot;
</code></pre>
<p>linsert <key> before/after <value> <newvalue> 在<value>的前面/后面插入<newvalue></p>
<pre><code class="language-shell">127.0.0.1:6379&gt; LINSERT k1 before &quot;v1&quot; &quot;v2&quot;
(integer) 4
127.0.0.1:6379&gt; LRANGE k1 0 -1
1) &quot;v3&quot;
2) &quot;v2&quot;
3) &quot;v2&quot;
4) &quot;v1&quot;
</code></pre>
<p>改变指定下标的值</p>
<pre><code class="language-shell">127.0.0.1:6379&gt; LSET k2 0 v10
OK
</code></pre>
<p>从左到右删除n个指定的value</p>
<pre><code class="language-shell">127.0.0.1:6379&gt; lrem  k1 2 &quot;v2&quot; 
(integer) 2
</code></pre>
<p>从左边或右边吐出一个值，值在键在，值光键亡</p>
<pre><code class="language-shell">127.0.0.1:6379&gt; LPOP k1
&quot;v3&quot;
127.0.0.1:6379&gt; LRANGE k1 0 -1
1) &quot;v1&quot;
127.0.0.1:6379&gt; RPOP k1
&quot;v1&quot;
127.0.0.1:6379&gt; LRANGE k1 0 -1
(empty array)
127.0.0.1:6379&gt; KEYS *
1) &quot;k2&quot;
</code></pre>
<p>从k1的右边吐出一个值，放在k2的左边</p>
<pre><code class="language-shell">127.0.0.1:6379&gt; LPUSH k1 v20 v21
(integer) 2
127.0.0.1:6379&gt; RPOPLPUSH k1 k2
&quot;v20&quot;
127.0.0.1:6379&gt; LRANGE k1 0 -1
1) &quot;v21&quot;
</code></pre>
<h3 id="数据结构-2">数据结构</h3>
<p>List的数据结构为快速列表<strong>quickList</strong>。</p>
<p>在列表元素较少时，会使用一块连续的内存存储，这个结构式<strong>zipList</strong>，即压缩列表。</p>
<p>当数据量比较多的时候才会使用<strong>quickList</strong>。</p>
<p>因为普通的链表需要的附加指针空间太大，会造成大量浪费。Redis将链表和zipList结合起来，既满足快速的插入删除性能，又不会出现太大的空间浪费。</p>
<figure data-type="image" tabindex="1"><a href="https://imgtu.com/i/LzthHe"><img src="https://s1.ax1x.com/2022/04/30/LzthHe.png" alt="LzthHe.png" loading="lazy"></a></figure>
<h2 id="set">Set</h2>
<h3 id="简介-3">简介</h3>
<ul>
<li>提供与List类似的列表功能，但是<strong>不允许出现重复元素</strong>。</li>
<li>可以判断元素是否在集合中。</li>
<li>无序集合，底层是一个value为null的hash表，所以添加，删除，查找的复杂度都是O(1)。</li>
</ul>
<h3 id="常用命令-3">常用命令</h3>
<p>将一个或多个member加入到key中，已经存在的忽略</p>
<pre><code class="language-shell">127.0.0.1:6379&gt; sadd k1 v1 v2 v3 v3
(integer) 3
</code></pre>
<p>查询指定key中的元素</p>
<pre><code class="language-shell">127.0.0.1:6379&gt; SMEMBERS k1
1) &quot;v2&quot;
2) &quot;v3&quot;
3) &quot;v1&quot;
</code></pre>
<p>判断value是否在key中</p>
<pre><code class="language-shell">127.0.0.1:6379&gt; SISMEMBER k1 v1
(integer) 1
</code></pre>
<p>查询key中元素个数</p>
<pre><code class="language-shell">127.0.0.1:6379&gt; SCARD k1
(integer) 3
</code></pre>
<p>从key中随机吐出一个元素</p>
<pre><code class="language-shell">127.0.0.1:6379&gt; spop k1
&quot;v2&quot;
</code></pre>
<p>从key中随机取出n个元素，不会从集合中删除</p>
<pre><code class="language-shell">127.0.0.1:6379&gt; SRANDMEMBER k1 2
1) &quot;v3&quot;
2) &quot;v1&quot;
</code></pre>
<p>删除集合中的一个或多个元素</p>
<pre><code class="language-shell">127.0.0.1:6379&gt; srem  k1 v3 v1
(integer) 2
</code></pre>
<p>返回两个集合的交集元素</p>
<pre><code class="language-shell">127.0.0.1:6379&gt; SADD k1 v1 v2 v3
(integer) 3
127.0.0.1:6379&gt; SADD k2 v2 v3 v4
(integer) 3
127.0.0.1:6379&gt; SINTER k1 k2
1) &quot;v2&quot;
2) &quot;v3&quot;
</code></pre>
<p>返回两个集合的并集</p>
<pre><code class="language-shell">127.0.0.1:6379&gt; SUNION k1 k2
1) &quot;v2&quot;
2) &quot;v4&quot;
3) &quot;v3&quot;
4) &quot;v1&quot;
</code></pre>
<p>返回k1中有，而k2中没有的元素</p>
<pre><code class="language-shell">127.0.0.1:6379&gt; SDIFF k1 k2
1) &quot;v1&quot;
</code></pre>
<p>把value从k1移动到k2中</p>
<pre><code class="language-shell">127.0.0.1:6379&gt; SMOVE k1 k2 v1
(integer) 1
127.0.0.1:6379&gt; SMEMBERS k1
1) &quot;v2&quot;
2) &quot;v3&quot;
</code></pre>
<h3 id="数据结构-3">数据结构</h3>
<p>Set数据结构是dict字典，用哈希表实现。</p>
<p>Java中的HashSet的内部实现使用的是HashMap，只不过所有的value都指向同一个对象。</p>
<p>Redis的set结构也是一样的，内部也使用hash结构，所有的value都指向同一个内部值。</p>
<h2 id="hash">Hash</h2>
<h3 id="简介-4">简介</h3>
<p>Redis的<strong>Hash</strong>是一个键值对集合。</p>
<p>Hash的value部分是一个<strong>field</strong>和<strong>value</strong>的映射表，所以，hash特别适合用于存储对象。</p>
<p>类似于Java中的Map&lt;String,Object&gt;。</p>
<ul>
<li>
<p>方法一</p>
<p>采用序列化操作，但要修改数据时，要先反序列化后在序列化，开销较大</p>
<figure data-type="image" tabindex="2"><a href="https://imgtu.com/i/O9kmkD"><img src="https://s1.ax1x.com/2022/05/01/O9kmkD.png" alt="O9kmkD.png" loading="lazy"></a></figure>
</li>
<li>
<p>方法二</p>
<p>重复存储用户id</p>
<figure data-type="image" tabindex="3"><a href="https://imgtu.com/i/O9k8nP"><img src="https://s1.ax1x.com/2022/05/01/O9k8nP.png" alt="O9k8nP.png" loading="lazy"></a></figure>
</li>
<li>
<p>方法三</p>
<p>最佳选择</p>
<figure data-type="image" tabindex="4"><a href="https://imgtu.com/i/O9kJ78"><img src="https://s1.ax1x.com/2022/05/01/O9kJ78.png" alt="O9kJ78.png" loading="lazy"></a></figure>
</li>
</ul>
<h3 id="常用命令-4">常用命令</h3>
<p>单个设置/取出数据</p>
<pre><code class="language-shell">127.0.0.1:6379&gt; HSET user01 id 1
(integer) 1
127.0.0.1:6379&gt; HSET user01 name zhangsan
(integer) 1
127.0.0.1:6379&gt; hget user01 id
&quot;1&quot;
127.0.0.1:6379&gt; hget user01 name
&quot;zhangsan&quot;
</code></pre>
<p>批量设置hash数据，查询key中所有的field，value</p>
<pre><code class="language-shell">127.0.0.1:6379&gt; hmset user02 id 2 name lisi
OK
127.0.0.1:6379&gt; HKEYS user02
1) &quot;id&quot;
2) &quot;name&quot;
127.0.0.1:6379&gt; HVALS user02
1) &quot;2&quot;
2) &quot;lisi&quot;
</code></pre>
<p>查询key中是否存在指定的field</p>
<pre><code class="language-shell">127.0.0.1:6379&gt; HEXISTS user02 age
(integer) 0
127.0.0.1:6379&gt; HEXISTS user02 id
(integer) 1
</code></pre>
<p>将哈希表 key 中的域 field 的值设置为 value ，当且仅当域 field 不存在</p>
<pre><code class="language-shell">127.0.0.1:6379&gt; HSETNX user02 age 18
(integer) 1
</code></pre>
<p>为哈希表 key 中的域 field 的值加上增量</p>
<pre><code class="language-shell">127.0.0.1:6379&gt; HINCRBY user02 age 2
(integer) 20
</code></pre>
<h3 id="数据结构-4">数据结构</h3>
<p>Hash类型对应的数据结构是两种：ziplist（压缩列表），hashtable（哈希表）。当field-value长度较短且个		数较少时，使用ziplist，否则使用hashtable。</p>
<h2 id="zset">Zset</h2>
<h3 id="简介-5">简介</h3>
<p>Zset和Set类似，是一个没有重复元素的字符串集合。</p>
<p>Zset中的每个成员都关联了一个<strong>评分(score)</strong>，这个评分被用来排序，从低到高。<em>集合中的成员是唯一的，但评分可以重复。</em></p>
<h3 id="常用命令-5">常用命令</h3>
<p>将一个或多个 member 元素及其 score 值加入到有序集 key 当中</p>
<pre><code class="language-shell">127.0.0.1:6379&gt; zadd topn 100 c 200 c++ 300 java
(integer) 3
</code></pre>
<p>返回有序集 key 中，下标在<start><stop>之间的元素</p>
<pre><code class="language-shell">127.0.0.1:6379&gt; zrange topn 0 -1
1) &quot;c&quot;
2) &quot;c++&quot;
3) &quot;java&quot;
#带WITHSCORES，可以让分数一起和值返回到结果集。
127.0.0.1:6379&gt; zrange topn 0 -1 withscores
1) &quot;c&quot;
2) &quot;100&quot;
3) &quot;c++&quot;
4) &quot;200&quot;
5) &quot;java&quot;
6) &quot;300&quot;
</code></pre>
<p>返回有序集 key 中，所有 score 值介于 min 和 max 之间(包括等于 min 或 max )的成员。有序集成员按 score 值递增(从小到大)次序排列</p>
<pre><code class="language-shell">127.0.0.1:6379&gt; zrangebyscore topn 101 300
1) &quot;c++&quot;
2) &quot;java&quot;
127.0.0.1:6379&gt; zrangebyscore topn 101 300 withscores
1) &quot;c++&quot;
2) &quot;200&quot;
3) &quot;java&quot;
4) &quot;300&quot;
#同上，改为从大到小排列
127.0.0.1:6379&gt; ZREVRANGEBYSCORE topn 300 100
1) &quot;java&quot;
2) &quot;c++&quot;
3) &quot;c&quot;
</code></pre>
<p>为元素的score加上增量</p>
<pre><code class="language-shell">127.0.0.1:6379&gt; ZINCRBY topn 10 java
&quot;310&quot;
</code></pre>
<p>统计该集合，分数区间内的元素个数</p>
<pre><code class="language-shell">127.0.0.1:6379&gt; ZCOUNT topn 100 300
(integer) 2
</code></pre>
<p>返回该值在集合中的排名，从0开始</p>
<pre><code class="language-shell">127.0.0.1:6379&gt; zrank topn c++
(integer) 1
</code></pre>
<p>删除该集合下，指定值的元素</p>
<pre><code class="language-shell">127.0.0.1:6379&gt; zrem  topn java 
(integer) 1
127.0.0.1:6379&gt; ZRANGE topn 0 -1
1) &quot;c&quot;
2) &quot;c++&quot;
</code></pre>
<h3 id="数据结构-5">数据结构</h3>
<p>SortedSet(zset)是Redis提供的一个非常特别的数据结构，一方面它等价于Java的数据结构Map&lt;String, Double&gt;，可以给每一个元素value赋予一个权重score，另一方面它又类似于TreeSet，内部的元素会按照权重score进行排序，可以得到每个元素的名次，还可以通过score的范围来获取元素的列表。</p>
<p>zset底层使用了两个数据结构</p>
<p>（1）hash，hash的作用就是关联元素value和权重score，保障元素value的唯一性，可以通过元素value找到相应的score值。</p>
<p>（2）跳跃表，跳跃表的目的在于给元素value排序，根据score的范围获取元素列表。</p>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[Redis之持久化操作]]></title>
        <id>https://Liangxi2001.github.io/post/redis-zhi-chi-jiu-hua-cao-zuo/</id>
        <link href="https://Liangxi2001.github.io/post/redis-zhi-chi-jiu-hua-cao-zuo/">
        </link>
        <updated>2022-05-19T08:35:53.000Z</updated>
        <content type="html"><![CDATA[<p><ul class="markdownIt-TOC">
<li>
<ul>
<li><a href="#1-rdbredis-database">1. RDB(Redis DataBase)</a>
<ul>
<li><a href="#1-%E7%AE%80%E4%BB%8B%E4%B8%8E%E6%B5%81%E7%A8%8B">1. 简介与流程</a></li>
<li><a href="#2-%E9%85%8D%E7%BD%AE%E6%96%87%E4%BB%B6">2. 配置文件</a></li>
<li><a href="#3-%E4%BC%98%E5%8A%BF">3. 优势</a></li>
<li><a href="#4-%E5%8A%A3%E5%8A%BF">4. 劣势</a></li>
<li><a href="#5-%E5%A4%87%E4%BB%BD">5. 备份</a></li>
</ul>
</li>
<li><a href="#2-aofappend-of-file">2. AOF(Append Of File)</a>
<ul>
<li><a href="#1-%E7%AE%80%E4%BB%8B%E4%B8%8E%E6%B5%81%E7%A8%8B-2">1. 简介与流程</a></li>
<li><a href="#2-%E9%85%8D%E7%BD%AE%E6%96%87%E4%BB%B6-2">2. 配置文件</a></li>
<li><a href="#3-aof%E5%90%AF%E5%8A%A8%E4%BF%AE%E5%A4%8D%E6%81%A2%E5%A4%8D">3. AOF启动/修复/恢复</a></li>
<li><a href="#4-%E5%90%8C%E6%AD%A5%E9%A2%91%E7%8E%87%E8%AE%BE%E7%BD%AE">4. 同步频率设置</a></li>
<li><a href="#5-rewrite%E9%87%8D%E5%86%99">5. Rewrite重写</a></li>
<li><a href="#6-%E4%BC%98%E5%8A%BF%E4%B8%8E%E5%8A%A3%E5%8A%BF">6. 优势与劣势</a>
<ul>
<li><a href="#%E4%BC%98%E5%8A%BF">优势</a></li>
<li><a href="#%E5%8A%A3%E5%8A%BF">劣势</a></li>
</ul>
</li>
</ul>
</li>
<li><a href="#3-%E6%80%BB%E7%BB%93">3. 总结</a></li>
</ul>
</li>
</ul>
</p>
<blockquote>
<p>Redis是基于内存的数据库，但它也可以把数据持久化到硬盘上。持久化的方式有两种：RDB和AOF。</p>
</blockquote>
<h2 id="1-rdbredis-database">1. RDB(Redis DataBase)</h2>
<h3 id="1-简介与流程">1. 简介与流程</h3>
<blockquote>
<p>RDB是什么？</p>
</blockquote>
<p><em>在指定的时间间隔内将内存中的数据集快照写入磁盘， 也就是行话讲的Snapshot快照，它恢复时是将快照文件直接读到内存里。</em></p>
<blockquote>
<p>如何备份数据？</p>
</blockquote>
<p>Redis会单独创建（fork）一个子进程来进行持久化，会先将数据写入到 一个临时文件中，待持久化过程都结束了，再用这个临时文件替换上次持久化好的文件。 整个过程中，主进程是不进行任何IO操作的，这就确保了极高的性能 如果需要进行大规模数据的恢复，且对于数据恢复的完整性不是非常敏感，那RDB方式要比AOF方式更加的高效。<strong>RDB</strong>的缺点是<strong>最后一次持久化后的数据可能丢失</strong>。</p>
<figure data-type="image" tabindex="1"><a href="https://imgtu.com/i/O7IunS"><img src="https://s1.ax1x.com/2022/05/19/O7IunS.png" alt="O7IunS.png" loading="lazy"></a></figure>
<ul>
<li>
<p>Fork的作用是复制一个与当前进程一样的进程。新进程的所有数据（变量、环境变量、程序计数器等） 数值都和原进程一致，但是是一个全新的进程，并作为原进程的子进程</p>
</li>
<li>
<p>在Linux程序中，fork()会产生一个和父进程完全相同的子进程，但子进程在此后多会exec系统调用，出于效率考虑，Linux中引入了“<strong>写时复制技术</strong>”</p>
</li>
<li>
<p><strong>一般情况父进程和子进程会共用同一段物理内存</strong>，只有进程空间的各段的内容要发生变化时，才会将父进程的内容复制一份给子进程。</p>
</li>
</ul>
<h3 id="2-配置文件">2. 配置文件</h3>
<p>在**/etc/redis.conf**中。</p>
<ol>
<li>
<p>dbfilename：设置备份文件的文件名</p>
</li>
<li>
<p>dir：rdb文件的保存路径，也可以修改。默认为Redis启动时命令行所在的目录下</p>
</li>
<li>
<p>save：save时只管保存，其它不管，全部阻塞。手动保存。不建议</p>
<p>save 秒钟 主键变化的次数：在n秒内有至少x个主键的值发生变化，那么进行备份</p>
</li>
<li>
<p>bgsave：Redis会在后台异步进行快照操作，快照同时还可以响应客户端请求。可以通过lastsave 命令<strong>获取最后一次成功执行快照的时间</strong></p>
</li>
<li>
<p>stop-writes-on-bgsave-error：当Redis无法写入磁盘的话，直接关掉Redis的写操作。推荐yes.</p>
</li>
<li>
<p>rdbcompression：对于存储到磁盘中的快照，可以设置是否进行压缩存储。如果是的话，redis会采用LZF算法进行压缩。</p>
<p>如果你不想消耗CPU来进行压缩的话，可以设置为关闭此功能。推荐yes.</p>
</li>
<li>
<p>rdbchecksum：在存储快照后，还可以让redis使用CRC64算法来进行<strong>数据校验</strong>，</p>
<p>但是这样做会增加大约10%的性能消耗，如果希望获取到最大的性能提升，可以关闭此功能</p>
<p>推荐yes.</p>
</li>
</ol>
<h3 id="3-优势">3. 优势</h3>
<ul>
<li>
<p>适合大规模的数据恢复</p>
</li>
<li>
<p>对数据完整性和一致性要求不高更适合使用</p>
</li>
<li>
<p>节省磁盘空间</p>
</li>
<li>
<p>恢复速度快</p>
</li>
</ul>
<h3 id="4-劣势">4. 劣势</h3>
<ul>
<li>
<p>Fork的时候，内存中的数据被克隆了一份，大致2倍的膨胀性需要考虑</p>
</li>
<li>
<p>虽然Redis在fork时使用了<strong>写时拷贝技术</strong>,但是如果数据庞大时还是比较消耗性能。</p>
</li>
<li>
<p>在备份周期在一定间隔时间做一次备份，所以如果Redis意外down掉的话，就会丢失最后一次快照后的所有修改。</p>
</li>
</ul>
<blockquote>
<p>如何停止？</p>
<p>动态停止RDB：redis-cli config set save &quot;&quot;#save后给空值，表示禁用保存策略</p>
</blockquote>
<figure data-type="image" tabindex="2"><a href="https://imgtu.com/i/O7oGUH"><img src="https://s1.ax1x.com/2022/05/19/O7oGUH.jpg" alt="O7oGUH.jpg" loading="lazy"></a></figure>
<h3 id="5-备份">5. 备份</h3>
<ol>
<li>复制dump.rdb文件，设为d.rdb；</li>
<li>在不注意删除dump.rdb文件后，只需要把d.rdb文件名改为dump.rdb即可。</li>
</ol>
<h2 id="2-aofappend-of-file">2. AOF(Append Of File)</h2>
<h3 id="1-简介与流程-2">1. 简介与流程</h3>
<blockquote>
<p>AOF是什么？</p>
</blockquote>
<p>以<strong>日志</strong>的形式来记录每个写操作（增量保存），将Redis执行过的所有写指令记录下来(<strong>读操作不记录</strong>)， <strong>只许追加文件但不可以改写文件</strong>，redis启动之初会读取该文件重新构建数据，换言之，redis 重启的话就根据日志文件的内容将写指令从前到后执行一次以完成数据的恢复工作。</p>
<blockquote>
<p>流程是怎样的？</p>
</blockquote>
<ol>
<li>
<p>客户端的请求写命令会被append追加到AOF缓冲区内；</p>
</li>
<li>
<p>AOF缓冲区根据AOF持久化策略[always,everysec,no]将操作sync同步到磁盘的AOF文件中；</p>
</li>
<li>
<p>AOF文件大小超过重写策略或手动重写时，会对AOF文件rewrite重写，压缩AOF文件容量；</p>
</li>
<li>
<p>Redis服务重启时，会重新load加载AOF文件中的写操作达到数据恢复的目的；</p>
</li>
</ol>
<figure data-type="image" tabindex="3"><a href="https://imgtu.com/i/O7vNT0"><img src="https://s1.ax1x.com/2022/05/19/O7vNT0.png" alt="O7vNT0.png" loading="lazy"></a></figure>
<h3 id="2-配置文件-2">2. 配置文件</h3>
<p><strong>AOF默认不开启</strong></p>
<p>可以在redis.conf中配置文件名称，默认为 appendonly.aof</p>
<p>AOF文件的保存路径，同RDB的路径一致。</p>
<blockquote>
<p>AOF和RDB同时开启时，redis听谁的？</p>
</blockquote>
<p>AOF和RDB同时开启，系统默认取AOF的数据（数据不会存在丢失）</p>
<h3 id="3-aof启动修复恢复">3. AOF启动/修复/恢复</h3>
<ul>
<li>
<p>l AOF的备份机制和性能虽然和RDB不同, 但是备份和恢复的操作同RDB一样，都是拷贝备份文件，需要恢复时再拷贝到Redis工作目录下，启动系统即加载。</p>
</li>
<li>
<p>正常恢复</p>
<ul>
<li>
<p>修改默认的appendonly no，改为yes</p>
</li>
<li>
<p>将有数据的aof文件复制一份保存到对应目录(查看目录：config get dir)</p>
</li>
<li>
<p>恢复：重启redis然后重新加载</p>
</li>
</ul>
</li>
<li>
<p>异常恢复</p>
<ul>
<li>
<p>修改默认的appendonly no，改为yes</p>
</li>
<li>
<p>如遇到<strong>AOF****文件损坏</strong>，通过/usr/local/bin/<strong>redis-check-aof--fix appendonly.aof</strong>进行恢复</p>
</li>
<li>
<p>备份被写坏的AOF文件</p>
</li>
<li>
<p>恢复：重启redis，然后重新加载</p>
</li>
</ul>
</li>
</ul>
<h3 id="4-同步频率设置">4. 同步频率设置</h3>
<ol>
<li>appendfsync always</li>
</ol>
<p>始终同步，每次Redis的写入都会立刻记入日志；性能较差但数据完整性比较好</p>
<ol start="2">
<li>appendfsync everysec</li>
</ol>
<p>每秒同步，每秒记入日志一次，如果宕机，本秒的数据可能丢失。</p>
<ol start="3">
<li>appendfsync no</li>
</ol>
<p>redis不主动进行同步，把同步时机交给操作系统。</p>
<h3 id="5-rewrite重写">5. Rewrite重写</h3>
<blockquote>
<p>Rewrite是什么？</p>
</blockquote>
<p>AOF采用文件追加方式，文件会越来越大为避免出现此种情况，新增了重写机制, 当AOF文件的大小超过所设定的阈值时，Redis就会启动AOF文件的内容压缩， 只保留可以恢复数据的最小指令集.可以使用命令<strong>bgrewriteaof</strong>。</p>
<blockquote>
<p>原理是什么？</p>
</blockquote>
<p>AOF文件持续增长而过大时，会fork出一条新进程来将文件重写(也是先写临时文件最后再rename)，redis4.0版本后的重写，是指上就是把rdb 的快照，以二级制的形式附在新的aof头部，作为已有的历史数据，替换掉原来的流水账操作。</p>
<p>Redis会记录上次重写时的AOF大小，默认配置是当AOF文件大小是上次rewrite后大小的一倍且文件大于64M时触发，例如：当阈值设置为64MB时，文件大小超过128MB，开启重写机制。</p>
<h3 id="6-优势与劣势">6. 优势与劣势</h3>
<h4 id="优势">优势</h4>
<ol>
<li>
<p>备份机制更稳健，丢失数据概率更低。</p>
</li>
<li>
<p>可读的日志文本，通过操作AOF稳健，可以处理误操作。</p>
</li>
</ol>
<h4 id="劣势">劣势</h4>
<ol>
<li>
<p>比起RDB占用更多的磁盘空间。</p>
</li>
<li>
<p>恢复备份速度要慢。</p>
</li>
<li>
<p>每次读写都同步的话，有一定的性能压力。</p>
</li>
<li>
<p>存在个别Bug，造成恢复不能。</p>
</li>
</ol>
<h2 id="3-总结">3. 总结</h2>
<p>官方推荐两个都启用。</p>
<p>如果对数据不敏感，可以选单独用RDB。</p>
<p>不建议单独用 AOF，因为可能会出现Bug。</p>
<p>如果只是做纯内存缓存，可以都不用。</p>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[SpringBoot整合Redis]]></title>
        <id>https://Liangxi2001.github.io/post/springboot-zheng-he-redis/</id>
        <link href="https://Liangxi2001.github.io/post/springboot-zheng-he-redis/">
        </link>
        <updated>2022-05-06T05:58:21.000Z</updated>
        <content type="html"><![CDATA[<p><ul class="markdownIt-TOC">
<li><a href="#1-%E5%88%9B%E5%BB%BA%E4%B8%80%E4%B8%AAspring%E9%A1%B9%E7%9B%AE">1. 创建一个Spring项目</a></li>
<li><a href="#2-%E5%BC%95%E5%85%A5%E4%BE%9D%E8%B5%96">2. 引入依赖</a></li>
<li><a href="#3-%E7%9B%B8%E5%85%B3%E9%85%8D%E7%BD%AE">3. 相关配置</a></li>
<li><a href="#4-%E5%9C%A8%E5%AE%B9%E5%99%A8%E4%B8%AD%E6%B3%A8%E5%85%A5bean">4. 在容器中注入Bean</a></li>
<li><a href="#5-%E6%B5%8B%E8%AF%95">5. 测试</a></li>
</ul>
</p>
<h1 id="1-创建一个spring项目">1. 创建一个Spring项目</h1>
<h1 id="2-引入依赖">2. 引入依赖</h1>
<pre><code class="language-xml">		&lt;dependency&gt;
            &lt;groupId&gt;org.springframework.boot&lt;/groupId&gt;
            &lt;artifactId&gt;spring-boot-starter-data-redis&lt;/artifactId&gt;
        &lt;/dependency&gt;

        &lt;!-- spring2.X集成redis所需common-pool2--&gt;
        &lt;dependency&gt;
            &lt;groupId&gt;org.apache.commons&lt;/groupId&gt;
            &lt;artifactId&gt;commons-pool2&lt;/artifactId&gt;
            &lt;version&gt;2.6.0&lt;/version&gt;
        &lt;/dependency&gt;
</code></pre>
<h1 id="3-相关配置">3. 相关配置</h1>
<pre><code class="language-properties">#Redis服务器地址
spring.redis.host=192.168.1.71
#Redis服务器连接端口
spring.redis.port=6379
#Redis数据库索引（默认为0）
spring.redis.database= 0
#连接超时时间（毫秒）
spring.redis.timeout=1800000
#连接池最大连接数（使用负值表示没有限制）
spring.redis.lettuce.pool.max-active=20
#最大阻塞等待时间(负数表示没限制)
spring.redis.lettuce.pool.max-wait=-1
#连接池中的最大空闲连接
spring.redis.lettuce.pool.max-idle=5
#连接池中的最小空闲连接
spring.redis.lettuce.pool.min-idle=0
</code></pre>
<h1 id="4-在容器中注入bean">4. 在容器中注入Bean</h1>
<pre><code class="language-java">/**
 * @author 此间凉汐
 * @date 2022/5/6 13:29
 */
@EnableCaching
@Configuration
public class RedisConfig extends CachingConfigurerSupport {

    @Bean
    public RedisTemplate&lt;String, Object&gt; redisTemplate(RedisConnectionFactory factory) {
        RedisTemplate&lt;String, Object&gt; template = new RedisTemplate&lt;&gt;();
        RedisSerializer&lt;String&gt; redisSerializer = new StringRedisSerializer();
        Jackson2JsonRedisSerializer jackson2JsonRedisSerializer = new Jackson2JsonRedisSerializer(Object.class);
        ObjectMapper om = new ObjectMapper();
        om.setVisibility(PropertyAccessor.ALL, JsonAutoDetect.Visibility.ANY);
        om.enableDefaultTyping(ObjectMapper.DefaultTyping.NON_FINAL);
        jackson2JsonRedisSerializer.setObjectMapper(om);
        template.setConnectionFactory(factory);
//key序列化方式
        template.setKeySerializer(redisSerializer);
//value序列化
        template.setValueSerializer(jackson2JsonRedisSerializer);
//value hashmap序列化
        template.setHashValueSerializer(jackson2JsonRedisSerializer);
        return template;
    }

    @Bean
    public CacheManager cacheManager(RedisConnectionFactory factory) {
        RedisSerializer&lt;String&gt; redisSerializer = new StringRedisSerializer();
        Jackson2JsonRedisSerializer jackson2JsonRedisSerializer = new Jackson2JsonRedisSerializer(Object.class);
//解决查询缓存转换异常的问题
        ObjectMapper om = new ObjectMapper();
        om.setVisibility(PropertyAccessor.ALL, JsonAutoDetect.Visibility.ANY);
        om.enableDefaultTyping(ObjectMapper.DefaultTyping.NON_FINAL);
        jackson2JsonRedisSerializer.setObjectMapper(om);
// 配置序列化（解决乱码的问题）,过期时间600秒
        RedisCacheConfiguration config = RedisCacheConfiguration.defaultCacheConfig()
                .entryTtl(Duration.ofSeconds(600))
                .serializeKeysWith(RedisSerializationContext.SerializationPair.fromSerializer(redisSerializer))
                .serializeValuesWith(RedisSerializationContext.SerializationPair.fromSerializer(jackson2JsonRedisSerializer))
                .disableCachingNullValues();
        RedisCacheManager cacheManager = RedisCacheManager.builder(factory)
                .cacheDefaults(config)
                .build();
        return cacheManager;
    }
}
</code></pre>
<h1 id="5-测试">5. 测试</h1>
<pre><code class="language-java">/**
 * @author 此间凉汐
 * @date 2022/5/6 13:37
 */
@RestController
@RequestMapping(&quot;/redisTest&quot;)
public class redisTestController {

    @Autowired
    private RedisTemplate redisTemplate;

    @GetMapping
    public String testRedis(){
        redisTemplate.opsForValue().set(&quot;name&quot;,&quot;liangxi&quot;);
        String name = (String) redisTemplate.opsForValue().get(&quot;name&quot;);
        return name;
    }
}
</code></pre>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[ 使用WebSocket实现在线聊天(后端)]]></title>
        <id>https://Liangxi2001.github.io/post/shi-yong-websocket-shi-xian-zai-xian-liao-tian-hou-duan/</id>
        <link href="https://Liangxi2001.github.io/post/shi-yong-websocket-shi-xian-zai-xian-liao-tian-hou-duan/">
        </link>
        <updated>2022-04-11T01:16:38.000Z</updated>
        <content type="html"><![CDATA[<p><ul class="markdownIt-TOC">
<li>
<ul>
<li><a href="#1websocket">1.WebSocket</a></li>
<li><a href="#2-java-websocket">2. java-websocket</a>
<ul>
<li><a href="#1-%E5%BC%95%E5%85%A5%E4%BE%9D%E8%B5%96">1. 引入依赖</a></li>
<li><a href="#2-websocket%E9%85%8D%E7%BD%AE%E7%B1%BB">2. WebSocket配置类</a></li>
<li><a href="#3-%E6%B6%88%E6%81%AF%E5%AE%9E%E4%BD%93%E7%B1%BB">3. 消息实体类</a></li>
<li><a href="#4-websocket%E6%8E%A7%E5%88%B6%E5%99%A8">4. websocket控制器</a></li>
<li><a href="#5-%E8%8E%B7%E5%8F%96%E8%81%8A%E5%A4%A9%E5%AF%B9%E8%B1%A1">5. 获取聊天对象</a></li>
</ul>
</li>
</ul>
</li>
</ul>
<br>
<em>后端使用websocket不需要太复杂的实现，而是起到中转站的效果，需配合前端使用。</em></p>
<h2 id="1websocket">1.WebSocket</h2>
<p>WebSocket 是 HTML5 开始提供的一种在单个 TCP 连接上进行全双工通讯的协议。</p>
<p>WebSocket 使得客户端和服务器之间的数据交换变得更加简单，允许服务端主动向客户端推送数据。在 WebSocket API 中，浏览器和服务器只需要完成一次握手，两者之间就直接可以创建持久性的连接，并进行双向数据传输。</p>
<p>在 WebSocket API 中，浏览器和服务器只需要做一个握手的动作，然后，浏览器和服务器之间就形成了一条快速通道。两者之间就直接可以数据互相传送。</p>
<p>现在，很多网站为了实现推送技术，所用的技术都是 Ajax 轮询。轮询是在特定的的时间间隔（如每1秒），由浏览器对服务器发出HTTP请求，然后由服务器返回最新的数据给客户端的浏览器。这种传统的模式带来很明显的缺点，即浏览器需要不断的向服务器发出请求，然而HTTP请求可能包含较长的头部，其中真正有效的数据可能只是很小的一部分，显然这样会浪费很多的带宽等资源。</p>
<p>HTML5 定义的 WebSocket 协议，能更好的节省服务器资源和带宽，并且能够更实时地进行通讯。</p>
<figure data-type="image" tabindex="1"><a href="https://imgtu.com/i/LEKY4K"><img src="https://s1.ax1x.com/2022/04/11/LEKY4K.png" alt="LEKY4K.png" loading="lazy"></a></figure>
<h2 id="2-java-websocket">2. java-websocket</h2>
<h3 id="1-引入依赖">1. 引入依赖</h3>
<pre><code class="language-xml">&lt;!--        websocket依赖--&gt;
        &lt;dependency&gt;
            &lt;groupId&gt;org.springframework.boot&lt;/groupId&gt;
            &lt;artifactId&gt;spring-boot-starter-websocket&lt;/artifactId&gt;
        &lt;/dependency&gt;
</code></pre>
<h3 id="2-websocket配置类">2. WebSocket配置类</h3>
<pre><code class="language-java">/**
 * WebSocket配置类
 * @author 此间凉汐
 * @date 2022/4/8 11:39
 */
@Configuration
@EnableWebSocketMessageBroker
public class WebSocketConfig implements WebSocketMessageBrokerConfigurer {

    @Value(&quot;${jwt.tokenHead}&quot;)
    private String tokenHead;

    @Autowired
    private JwtTokenUtil jwtTokenUtil;
    @Autowired
    private UserDetailsService userDetailsService;

    /**
     * 1.将/ws/ep注册为stomp的端点，用户连接了这个端点就可以进行websocket通讯，支持socketJS
     * 2.setAllowedOrigins(&quot;*&quot;):允许跨域
     * 3.withSockJS():支持socketJS访问
     *
     * @param registry
     */
    @Override
    public void registerStompEndpoints(StompEndpointRegistry registry) {
        registry.addEndpoint(&quot;/ws/ep&quot;).setAllowedOrigins(&quot;*&quot;).withSockJS();
    }

    /**
     * 输入管道参数，因为使用了jwt
     * @param registration
     */
    @Override
    public void configureClientInboundChannel(ChannelRegistration registration) {
        registration.interceptors(new ChannelInterceptor() {
            @Override
            public Message&lt;?&gt; preSend(Message&lt;?&gt; message, MessageChannel channel) {
                StompHeaderAccessor accessor = MessageHeaderAccessor.getAccessor(message, StompHeaderAccessor.class);
                //判断是否为连接，如果是，需要获取token，并且设置用户对象
                if (StompCommand.CONNECT.equals(accessor.getCommand())) {
                    String token = accessor.getFirstNativeHeader(&quot;Auth-Token&quot;);
                    if (!StringUtils.isEmpty(token)) {
                        String authToken = token.substring(tokenHead.length());
                        String username = jwtTokenUtil.getUserNameFromToken(authToken);
                        //token中存在用户名
                        if (!StringUtils.isEmpty(username)) {
                            //登录
                            UserDetails userDetails = userDetailsService.loadUserByUsername(username);
                            //验证token是否有效，重新设置用户对象
                            if (jwtTokenUtil.validateToken(authToken, userDetails)) {
                                UsernamePasswordAuthenticationToken authenticationToken = new UsernamePasswordAuthenticationToken(userDetails, null, userDetails.getAuthorities());
                                SecurityContextHolder.getContext().setAuthentication(authenticationToken);
                                accessor.setUser(authenticationToken);
                            }
                        }
                    }
                }
                return message;
            }
        });
    }

    /**
     * 配置消息代理
     *
     * @param registry
     */
    @Override
    public void configureMessageBroker(MessageBrokerRegistry registry) {
        //配置代理域，可以配置多个，配置代理目的地前缀为/queue，可以在配置域上向客户端推送消息
        registry.enableSimpleBroker(&quot;/queue&quot;);
    }
}
</code></pre>
<h3 id="3-消息实体类">3. 消息实体类</h3>
<blockquote>
<p>发送者，接收者，内容，发送时间，发送者昵称</p>
</blockquote>
<pre><code class="language-java">/**
 * 消息
 * @author 此间凉汐
 * @date 2022/4/8 14:17
 */
@Data
@EqualsAndHashCode(callSuper = false)
@Accessors(chain = true)
public class ChatMsg {

    private String from;
    private String to;
    private String content;
    private LocalDateTime date;
    private String formNickName;
}
</code></pre>
<h3 id="4-websocket控制器">4. websocket控制器</h3>
<pre><code class="language-java">/**
 * websocket
 * @author 此间凉汐
 * @date 2022/4/8 14:22
 */
@Controller
public class WsController {

    @Autowired
    private SimpMessagingTemplate simpMessagingTemplate;

    @MessageMapping(&quot;/ws/chat&quot;)
    public void handleMsg(Authentication authentication, ChatMsg chatMsg){
        Admin admin = (Admin) authentication.getPrincipal();
        chatMsg.setFrom(admin.getUsername());
        chatMsg.setFormNickName(admin.getName());
        chatMsg.setDate(LocalDateTime.now());
        simpMessagingTemplate.convertAndSendToUser(chatMsg.getTo(),&quot;/queue/chat&quot;,chatMsg);
    }
}
</code></pre>
<h3 id="5-获取聊天对象">5. 获取聊天对象</h3>
<pre><code class="language-java">/**
 * 在线聊天
 * @author 此间凉汐
 * @date 2022/4/8 14:30
 */
@RestController
@RequestMapping(&quot;/chat&quot;)
public class ChatController {

    @Autowired
    private IAdminService adminService;

    @ApiOperation(value = &quot;获取所有操作员&quot;)
    @GetMapping(&quot;/admin&quot;)
    public List&lt;Admin&gt; getAllAdmins(String keywords){
        return adminService.getAllAdmins(keywords);
    }
}
</code></pre>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[邮件传送(RabbitMQ消息队列)]]></title>
        <id>https://Liangxi2001.github.io/post/you-jian-chuan-song/</id>
        <link href="https://Liangxi2001.github.io/post/you-jian-chuan-song/">
        </link>
        <updated>2022-04-10T12:41:04.000Z</updated>
        <summary type="html"><![CDATA[<p>当新员工入职时，在将其信息保存到数据库的同时，给其发送入职欢迎邮件，并将此邮件也保存到数据库，当status为0时，表示正在发送；为1时，表示发送成功；如果重发次数大于3，status将变为2，表示发送失败，不再发送。</p>
]]></summary>
        <content type="html"><![CDATA[<p>当新员工入职时，在将其信息保存到数据库的同时，给其发送入职欢迎邮件，并将此邮件也保存到数据库，当status为0时，表示正在发送；为1时，表示发送成功；如果重发次数大于3，status将变为2，表示发送失败，不再发送。</p>
<!-- more -->
<p><ul class="markdownIt-TOC">
<li>
<ul>
<li><a href="#1-%E9%82%AE%E4%BB%B6%E5%8F%91%E9%80%81">1. 邮件发送</a>
<ul>
<li><a href="#1%E5%88%9B%E5%BB%BA%E9%82%AE%E4%BB%B6%E5%8F%91%E9%80%81%E6%9C%8D%E5%8A%A1%E5%99%A8">1.创建邮件发送服务器</a></li>
<li><a href="#2-%E6%9C%8D%E5%8A%A1%E5%99%A8%E5%8F%91%E5%87%BA%E9%82%AE%E4%BB%B6">2. 服务器发出邮件</a></li>
<li><a href="#3-%E7%A1%AE%E8%AE%A4%E9%82%AE%E4%BB%B6%E5%8F%91%E9%80%81%E5%87%BA%E5%8E%BB">3. 确认邮件发送出去</a></li>
</ul>
</li>
<li><a href="#2-%E7%A1%AE%E4%BF%9D%E5%8F%AF%E9%9D%A0%E6%80%A7%E5%AE%9E%E7%8E%B0%E9%87%8D%E4%BC%A0">2. 确保可靠性，实现重传</a>
<ul>
<li><a href="#1-%E5%87%86%E5%A4%87%E4%B8%80%E4%B8%AA%E5%B8%B8%E9%87%8F%E7%B1%BB">1. 准备一个常量类</a></li>
<li><a href="#2%E9%82%AE%E4%BB%B6%E5%AE%9A%E6%97%B6%E9%87%8D%E4%BC%A0">2.邮件定时重传</a></li>
</ul>
</li>
</ul>
</li>
</ul>
</p>
<h2 id="1-邮件发送">1. 邮件发送</h2>
<blockquote>
<p>场景：在有新员工入职时，会自动给其发送入职欢迎邮件，使用RabbitMQ实现</p>
</blockquote>
<h3 id="1创建邮件发送服务器">1.创建邮件发送服务器</h3>
<ol>
<li>
<p>创建一个子模块</p>
<ul>
<li>
<p>pom依赖</p>
<pre><code class="language-xml">&lt;?xml version=&quot;1.0&quot; encoding=&quot;UTF-8&quot;?&gt;

&lt;project xmlns=&quot;http://maven.apache.org/POM/4.0.0&quot; xmlns:xsi=&quot;http://www.w3.org/2001/XMLSchema-instance&quot;
         xsi:schemaLocation=&quot;http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd&quot;&gt;
    &lt;parent&gt;
        &lt;artifactId&gt;yeb&lt;/artifactId&gt;
        &lt;groupId&gt;com.yebProject&lt;/groupId&gt;
        &lt;version&gt;0.0.1-SNAPSHOT&lt;/version&gt;
    &lt;/parent&gt;
    &lt;modelVersion&gt;4.0.0&lt;/modelVersion&gt;

    &lt;artifactId&gt;yeb-mail&lt;/artifactId&gt;

    &lt;name&gt;yeb-mail&lt;/name&gt;
    &lt;!-- FIXME change it to the project's website --&gt;
    &lt;url&gt;http://www.example.com&lt;/url&gt;

    &lt;properties&gt;
        &lt;project.build.sourceEncoding&gt;UTF-8&lt;/project.build.sourceEncoding&gt;
        &lt;maven.compiler.source&gt;1.8&lt;/maven.compiler.source&gt;
        &lt;maven.compiler.target&gt;1.8&lt;/maven.compiler.target&gt;
    &lt;/properties&gt;

    &lt;dependencies&gt;
        &lt;!--    RabbitMQ依赖--&gt;
        &lt;dependency&gt;
            &lt;groupId&gt;org.springframework.boot&lt;/groupId&gt;
            &lt;artifactId&gt;spring-boot-starter-amqp&lt;/artifactId&gt;
        &lt;/dependency&gt;
        &lt;!--    mail依赖--&gt;
        &lt;dependency&gt;
            &lt;groupId&gt;org.springframework.boot&lt;/groupId&gt;
            &lt;artifactId&gt;spring-boot-starter-mail&lt;/artifactId&gt;
        &lt;/dependency&gt;
        &lt;!--        thymeleaf依赖--&gt;
        &lt;dependency&gt;
            &lt;groupId&gt;org.springframework.boot&lt;/groupId&gt;
            &lt;artifactId&gt;spring-boot-starter-thymeleaf&lt;/artifactId&gt;
        &lt;/dependency&gt;
&lt;!--        server依赖--&gt;
        &lt;dependency&gt;
            &lt;groupId&gt;com.yebProject&lt;/groupId&gt;
            &lt;artifactId&gt;yeb-server&lt;/artifactId&gt;
            &lt;version&gt;0.0.1-SNAPSHOT&lt;/version&gt;
        &lt;/dependency&gt;
    &lt;/dependencies&gt;
&lt;/project&gt;
</code></pre>
</li>
<li>
<p>项目结构：</p>
</li>
</ul>
<figure data-type="image" tabindex="1"><a href="https://imgtu.com/i/LA1S0A"><img src="https://s1.ax1x.com/2022/04/10/LA1S0A.png" alt="LA1S0A.png" loading="lazy"></a></figure>
<ul>
<li>
<p>配置文件</p>
<pre><code class="language-yaml">#启动端口，使用邮件传输服务需要启动mail和server
server:
  port: 8082

spring:
  mail:
#    邮件服务器地址
    host: smtp.qq.com
#    协议
    protocol: smtp
#    默认编码
    default-encoding: utf-8
#    授权码（开启smtp服务时获取）
    password: orutrptlzytpdiec
#    发送者邮件地址
    username: 2373162039@qq.com
#    端口
    port: 25

  rabbitmq:
    username: guest
    password: guest
#    服务器地址
    host: localhost
#    端口
    port: 5672
</code></pre>
</li>
<li>
<p>邮件模板</p>
<p>邮件使用thymeleaf模板</p>
<pre><code class="language-html">&lt;!DOCTYPE html&gt;
&lt;html lang=&quot;en&quot; xmlns:th=&quot;http://www.thymeleaf.org&quot;&gt;
&lt;head&gt;
    &lt;meta charset=&quot;UTF-8&quot;&gt;
    &lt;title&gt;入职欢迎邮件&lt;/title&gt;
&lt;/head&gt;
&lt;body&gt;
    欢迎&lt;span th:text=&quot;${name}&quot;&gt;&lt;/span&gt;加入xxx大家庭，您的入职信息如下：
&lt;table border=&quot;1&quot;&gt;
    &lt;tr&gt;
        &lt;td&gt;姓名&lt;/td&gt;
        &lt;td th:text=&quot;${name}&quot;&gt;&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
        &lt;td&gt;职位&lt;/td&gt;
        &lt;td th:text=&quot;${posName}&quot;&gt;&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
        &lt;td&gt;职称&lt;/td&gt;
        &lt;td th:text=&quot;${joblevelName}&quot;&gt;&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
        &lt;td&gt;部门&lt;/td&gt;
        &lt;td th:text=&quot;${departmentName}&quot;&gt;&lt;/td&gt;
    &lt;/tr&gt;
&lt;/table&gt;
&lt;p&gt;最后，再一次欢迎您加入我们！&lt;/p&gt;
&lt;/body&gt;
&lt;/html&gt;
</code></pre>
</li>
<li>
<p>消息接收者</p>
<pre><code class="language-java">/**
 * 消息接收者
 * @author 此间凉汐
 * @date 2022/4/5 14:08
 */
@Component
public class MailReceiver {
    private static final Logger LOGGER = LoggerFactory.getLogger(MailReceiver.class);
    @Autowired
    private JavaMailSender javaMailSender;
    @Autowired
    private MailProperties mailProperties;
    @Autowired
    private TemplateEngine templateEngine;

    @RabbitListener(queues = MailConstants.MAIL_QUEUE_NAME)
    public void handler(Employee employee){
        MimeMessage msg = javaMailSender.createMimeMessage();
        MimeMessageHelper helper = new MimeMessageHelper(msg);
        try {
            helper.setFrom(mailProperties.getUsername());
            helper.setTo(employee.getEmail());
            helper.setSubject(&quot;入职欢迎邮件&quot;);
            helper.setSentDate(new Date());
            Context context = new Context();
            context.setVariable(&quot;name&quot;,employee.getName());
            context.setVariable(&quot;posName&quot;,employee.getPosition().getName());
            context.setVariable(&quot;joblevelName&quot;,employee.getJoblevel().getName());
            context.setVariable(&quot;departmentName&quot;,employee.getDepartment().getName());
            //把context放入邮件模板中
            String mail = templateEngine.process(&quot;mail&quot;, context);
            helper.setText(mail,true);
            javaMailSender.send(msg);
        } catch (MessagingException e) {
            LOGGER.error(&quot;邮件发送失败========&gt;{}&quot;,e.getMessage());
        }
    }
}
</code></pre>
</li>
<li>
<p>启动类</p>
<pre><code class="language-java">/**
 * @author 此间凉汐
 * @date 2022/4/5 13:19
 */
@SpringBootApplication(exclude = {DataSourceAutoConfiguration.class})
public class MailApplication {
    public static void main(String[] args) {
        SpringApplication.run(MailApplication.class,args);
    }

    @Bean
    public  Queue queue(){
        return new Queue(MailConstants.MAIL_QUEUE_NAME);
    }
}
</code></pre>
</li>
</ul>
<h3 id="2-服务器发出邮件">2. 服务器发出邮件</h3>
<ul>
<li>
<p>server中相关配置</p>
<pre><code class="language-yaml">  rabbitmq:
#    rabbitmq默认的用户与密码
    username: guest
    password: guest
    #    服务器地址
    host: localhost
    #    端口
    port: 5672
#    消息确认回调
    publisher-confirm-type: correlated
#    消息失败回调
    publisher-returns: true
</code></pre>
</li>
<li>
<p>在server子项目的pom中导入RabbitMQ依赖</p>
<pre><code class="language-xml">&lt;!--    RabbitMQ依赖--&gt;
&lt;dependency&gt;
    &lt;groupId&gt;org.springframework.boot&lt;/groupId&gt;
    &lt;artifactId&gt;spring-boot-starter-amqp&lt;/artifactId&gt;
&lt;/dependency&gt;
</code></pre>
</li>
<li>
<p>在添加员工的同时发送邮件</p>
<pre><code class="language-java">/**
 * @author liangxi
 * @since 2022-03-22
 */
@Service
public class EmployeeServiceImpl extends ServiceImpl&lt;EmployeeMapper, Employee&gt; implements IEmployeeService {

    @Autowired
    private EmployeeMapper employeeMapper;
    @Autowired
    private RabbitTemplate rabbitTemplate;
    @Autowired
    private MailLogMapper mailLogMapper;

    /**
     * 获取所有员工（分页）
     * @param current
     * @param size
     * @param employee
     * @param beginDateScope
     * @return
     */
    @Override
    public RespPageBean getEmployeeByPage(Integer current, Integer size, Employee employee, LocalDate[] beginDateScope) {
        //开启分页
        Page&lt;Employee&gt; page = new Page&lt;&gt;(current, size);
        IPage&lt;Employee&gt; employeeByPage = employeeMapper.getEmployeeByPage(page, employee, beginDateScope);
        RespPageBean respPageBean = new RespPageBean(employeeByPage.getTotal(), employeeByPage.getRecords());
        return respPageBean;
    }

    /**
     * 获取工号
     * @return
     */
    @Override
    public RespBean maxWorkID() {
        List&lt;Map&lt;String, Object&gt;&gt; maps = employeeMapper.selectMaps(new QueryWrapper&lt;Employee&gt;().select(&quot;max(workID)&quot;));
        return RespBean.success(null, String.format(&quot;%08d&quot;,
                Integer.parseInt(maps.get(0).get(&quot;max(workID)&quot;).toString()) + 1));
    }

    /**
     * 添加员工,并发送入职邮件
     * @param employee
     * @return
     */
    @Override
    public RespBean addEmp(Employee employee) {
        LocalDate beginContract = employee.getBeginContract();
        LocalDate endContract = employee.getEndContract();
        long days = beginContract.until(endContract, ChronoUnit.DAYS);
        DecimalFormat decimalFormat = new DecimalFormat(&quot;##.00&quot;);
        employee.setContractTerm(Double.parseDouble(decimalFormat.format(days / 265.00)));
        if (1 == employeeMapper.insert(employee)){
            Employee emp = employeeMapper.getEmployee(employee.getId()).get(0);
            //发送的消息入库
            String msgId = UUID.randomUUID().toString();
            MailLog mailLog = new MailLog();
            mailLog.setMsgId(msgId);
            mailLog.setEid(employee.getId());
            mailLog.setStatus(0);
            mailLog.setRouteKey(MailConstants.MAIL_ROUTING_KEY_NAME);
            mailLog.setExchange(MailConstants.MAIL_EXCHANGE_NAME);
            mailLog.setCount(0);
            mailLog.setTryTime(LocalDateTime.now().plusMinutes(MailConstants.MSG_TIMEOUT));
            mailLog.setCreateTime(LocalDateTime.now());
            mailLog.setUpdateTime(LocalDateTime.now());
            mailLogMapper.insert(mailLog);
            //发送信息
            rabbitTemplate.convertAndSend(MailConstants.MAIL_EXCHANGE_NAME,MailConstants.MAIL_ROUTING_KEY_NAME,
                    emp,new CorrelationData(msgId));
            return RespBean.success(&quot;添加成功！&quot;);
        }
        return RespBean.error(&quot;添加失败！&quot;);
    }
}
</code></pre>
</li>
</ul>
<h3 id="3-确认邮件发送出去">3. 确认邮件发送出去</h3>
<pre><code class="language-java">/**
 * RabbitMQ配置类
 *
 * @author 此间凉汐
 * @date 2022/4/6 9:00
 */
@Configuration
public class RabbitMQConfig {

    private static final Logger LOGGER = LoggerFactory.getLogger(RabbitMQConfig.class);
    @Autowired
    private CachingConnectionFactory cachingConnectionFactory;
    @Autowired
    private IMailLogService mailLogService;

    @Bean
    public RabbitTemplate rabbitTemplate() {
        RabbitTemplate rabbitTemplate = new RabbitTemplate(cachingConnectionFactory);

        /**
         * 消息确认回调，确认消息是否到达broker
         * data：消息唯一标识
         * ack：确认结果
         * cause：失败原因
         */
        rabbitTemplate.setConfirmCallback((data, ack, cause) -&gt; {
            String msgId = data.getId();
            if (ack) {
                LOGGER.info(&quot;{}===========&gt;消息发送成功&quot;, msgId);
                mailLogService.update(new UpdateWrapper&lt;MailLog&gt;().set(&quot;status&quot;, 1).eq(&quot;msgId&quot;, msgId));
            } else {
                LOGGER.error(&quot;{}===========&gt;消息发送失败&quot;, msgId);
            }
        });

        /**
         * 消息失败回调
         * msg:消息主题
         * repCode:响应码
         * repText:响应描述
         * exchange:交换机
         * routingKey:路由键
         */
        rabbitTemplate.setReturnCallback((msg, repCode, repText, exchange, routingKey) -&gt; {
            LOGGER.error(&quot;{}===========&gt;消息发送queue时失败&quot;, msg.getBody());
        });
        return rabbitTemplate;
    }

    @Bean
    public Queue queue() {
        return new Queue(MailConstants.MAIL_QUEUE_NAME);
    }

    @Bean
    public DirectExchange directExchange() {
        return new DirectExchange(MailConstants.MAIL_EXCHANGE_NAME);
    }

    @Bean
    public Binding binding() {
        //消息队列和交换机通过路由键绑定在一起
        return BindingBuilder.bind(queue()).to(directExchange()).with(MailConstants.MAIL_ROUTING_KEY_NAME);
    }
}
</code></pre>
<h2 id="2-确保可靠性实现重传">2. 确保可靠性，实现重传</h2>
<h3 id="1-准备一个常量类">1. 准备一个常量类</h3>
<pre><code class="language-java">package com.yebProject.server.pojo;

/**
 * 消息状态
 * @author 此间凉汐
 * @date 2022/4/6 8:41
 */
public class MailConstants {

    //消息投递中
    public static final Integer DELIVERING = 0;

    //消息投递成功
    public static final Integer SUCCESS = 1;

    //消息投递失败
    public static final Integer FAILURE = 2;

    //最大重试次数
    public static final Integer MAX_TRY_COUNT = 3;

    //消息超时时间
    public static final Integer MSG_TIMEOUT = 1;

    //队列
    public static final String MAIL_QUEUE_NAME = &quot;mail.queue&quot;;

    //交换机
    public static final String MAIL_EXCHANGE_NAME = &quot;mail.exchange&quot;;

    //路由键
    public static final String MAIL_ROUTING_KEY_NAME = &quot;mail.routing.key&quot;;
}
</code></pre>
<h3 id="2邮件定时重传">2.邮件定时重传</h3>
<pre><code class="language-java">/**
 * 邮件定时重传
 * @author 此间凉汐
 * @date 2022/4/7 8:35
 */
@Component
public class MailTask {
    @Autowired
    private IMailLogService mailLogService;
    @Autowired
    private IEmployeeService employeeService;
    @Autowired
    private RabbitTemplate rabbitTemplate;

    /**
     * 10秒执行一次
     */
    @Scheduled(cron = &quot;0/10 * * * * ?&quot;)
    public void mailTask(){
        List&lt;MailLog&gt; list = mailLogService.list(new QueryWrapper&lt;MailLog&gt;().eq(&quot;status&quot;, 0)
                .lt(&quot;tryTime&quot;, LocalDateTime.now()));
        list.forEach(mailLog -&gt; {
            //如果重试次数大于2，更新状态为投递失败，不再重试
            if (3&lt;=mailLog.getCount()){
                mailLogService.update(new UpdateWrapper&lt;MailLog&gt;().set(&quot;status&quot;,2)
                        .eq(&quot;msgId&quot;,mailLog.getMsgId()));
            }
            mailLogService.update(new UpdateWrapper&lt;MailLog&gt;().set(&quot;count&quot;,mailLog.getCount()+1)
                    .set(&quot;updateTime&quot;,LocalDateTime.now())
                    .set(&quot;tryTime&quot;,LocalDateTime.now().plusMinutes(MailConstants.MSG_TIMEOUT))
                    .eq(&quot;msgId&quot;,mailLog.getMsgId()));
            Employee emp = employeeService.getEmployee(mailLog.getEid()).get(0);
            //发送消息
            rabbitTemplate.convertAndSend(MailConstants.MAIL_EXCHANGE_NAME,MailConstants.MAIL_ROUTING_KEY_NAME
            ,emp,new CorrelationData(mailLog.getMsgId()));
        });
    }
}
</code></pre>
<p><em>记得开启启动类的定时任务</em></p>
</li>
</ol>
<pre><code class="language-java">@SpringBootApplication
@MapperScan(&quot;com.yebProject.server.mapper&quot;)
//@EnableScheduling开启定时任务
@EnableScheduling
public class YebApplication {
    public static void main(String[] args) {
        SpringApplication.run(YebApplication.class, args);
    }
}
</code></pre>
]]></content>
    </entry>
</feed>